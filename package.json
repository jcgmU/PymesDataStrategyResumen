{
  "name": "pymes-data-strategy",
  "version": "0.1.0",
  "private": true,
  "description": "ETL system with Human-in-the-Loop for AI-assisted data cleaning",
  "type": "module",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "dev": "pnpm -r run dev",
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint",
    "typecheck": "pnpm -r run typecheck",
    "format": "pnpm -r run format"
  },
  "devDependencies": {
    "prisma": "6.4.1"
  },
  "dependencies": {
    "@prisma/client": "6.4.1"
  }
}
